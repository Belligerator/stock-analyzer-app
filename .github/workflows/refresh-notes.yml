name: Refresh AI notes

on:
  # Scheduled cron is intentionally disabled for now.
  # Run manually from GitHub Actions -> Refresh AI notes -> Run workflow.
  workflow_dispatch: # manual trigger from GitHub UI

concurrency:
  group: refresh-notes
  cancel-in-progress: false

jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Call Vercel cron endpoint
        env:
          URL: ${{ secrets.VERCEL_URL }}
          SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$URL" ] || [ -z "$SECRET" ]; then
            echo "Missing VERCEL_URL or CRON_SECRET in repo secrets" >&2
            exit 1
          fi
          response=$(curl -sS --max-time 300 -w "\n%{http_code}" -H "Authorization: Bearer $SECRET" "$URL/api/cron/refresh-notes")
          body=$(echo "$response" | sed '$d')
          code=$(echo "$response" | tail -n1)
          echo "$body"
          if [ "$code" != "200" ]; then
            echo "HTTP $code" >&2
            exit 1
          fi
